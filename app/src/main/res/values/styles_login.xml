<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ==================== ESTILOS DE LOGIN / AUTENTICACIÓN ==================== -->

    <!-- Título principal de pantallas de autenticación -->
    <style name="TextTitle">
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">32sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Texto secundario/muted para pantallas de autenticación -->
    <style name="TextMuted">
        <item name="android:textColor">@color/text_muted</item>
    </style>

    <!-- Campo de entrada de texto para formularios de autenticación -->
    <style name="WidgetAppInput">
        <item name="android:background">@drawable/bg_input</item>
        <item name="android:textColor">@color/text_primary</item>
        <item name="android:textColorHint">@color/text_hint</item>
        <item name="android:paddingStart">20dp</item>
        <item name="android:paddingEnd">20dp</item>
    </style>

    <!-- Botón principal para pantallas de autenticación -->
    <style name="WidgetAppPrimaryButton" parent="Widget.Material3.Button">
        <item name="android:background">@drawable/bg_button</item>
        <item name="backgroundTint">@null</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
    </style>

</resources>

